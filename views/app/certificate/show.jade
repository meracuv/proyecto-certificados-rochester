extends ../../layout.jade
block contenido
	div(class="img-container")
		h1= imagen.title
		embed(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",width="100%",height="500")
		p Por #{imagen.creator.email}